#using equal on operators
var1 = "hello"
print(var1)

#using leftward operator
var2 <- "hello"
print(var2)

#using rightward operator
"hello" -> var3
print(var3)

#notes when creating a variable name
#underscore "_" and period "." are the only special characters allowed
#variable names can start with alphabets and dot
#variable names cannot start with numbers and underscore
#if the variable name starts with a period ".", the next character should not be a number
#a variable name should never be name TRUE or FALSE as these words are reserved keywords. 